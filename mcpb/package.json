{
  "name": "socialchamp-mcp-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Claude Desktop Extension for Social Champ (OAuth via mcp-remote)",
  "dependencies": {
    "mcp-remote": "^0.1.0"
  }
}
